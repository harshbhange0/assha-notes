import { Router } from "express";

const router = Router();

router.post("create");
router.put("update");
router.put("add-kid");
router.delete("delete");
router.get("get-house");
export default router;
